my_list = ['a', 'c', 'd']

# test = 'o'.join(my_list)
test = 'o'.join()
print(test)

# Entering additional arguments: TypeError. Takes exactly 1 argument.
# Entering too few: Same error: '0 given' for arguments.