falsy = [
    False, None (), [], {}, set(), frozenset(), '',
    0, 0.0, 0j, # all zero numeric types
]

print(falsy)

# needed to add empty range, empty string

# Falsy is objects that evaluate as falsy in a boolean context.